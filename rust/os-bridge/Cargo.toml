[package]
name = "os-bridge"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]
path = "src/lib.rs"

[dependencies]
fms-core = { path = "../fms-core" }
kernel-modules = { path = "../kernel-modules" }
